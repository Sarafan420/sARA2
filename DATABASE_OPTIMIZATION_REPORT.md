# 📊 ОТЧЕТ О ПРОВЕРКЕ БАЗЫ ДАННЫХ

## Выполнено: Проверка базы данных на лишние таблицы и неиспользуемые столбцы

---

## ✅ РЕЗУЛЬТАТЫ ПРОВЕРКИ

### 1. Неиспользуемые таблицы: **0**
Все таблицы используются в проекте.

### 2. Неиспользуемые столбцы: **3**

#### Vacancy.hoursPerWeek
- **Статус**: Не используется в коде
- **Рекомендация**: Можно удалить или использовать для будущей функциональности

#### Vacancy.contactEmail
- **Статус**: Не используется в коде
- **Рекомендация**: Можно удалить или использовать для контактной информации

#### Vacancy.telegramUsername
- **Статус**: Не используется в коде
- **Рекомендация**: Можно удалить или использовать для контактной информации

### 3. Устаревшие столбцы: **1**

#### User.skills
- **Статус**: Помечен как `deprecated` в схеме, но все еще используется (15 использований)
- **Проблема**: Данные хранятся в JSON формате, новая система использует таблицу `UserSkill`
- **Рекомендация**: 
  1. Мигрировать данные из `User.skills` в `UserSkill`
  2. Обновить код для использования `UserSkill` вместо `User.skills`
  3. Удалить столбец `User.skills` после миграции

### 4. Дублирующиеся столбцы: **2**

#### Vacancy: skillsRequired и VacancySkill (relation)
- **Проблема**: Навыки вакансий хранятся двумя способами:
  - `Vacancy.skillsRequired` (JSON строка)
  - `VacancySkill` (связующая таблица через relation)
- **Использование**: Оба способа используются в коде
- **Рекомендация**: 
  1. Мигрировать данные из `skillsRequired` в `VacancySkill`
  2. Обновить код для использования только `VacancySkill`
  3. Удалить столбец `skillsRequired` после миграции

#### profileVisibility в UserPrivacySettings и UserProfileSettings
- **Проблема**: Поле `profileVisibility` дублируется в двух таблицах настроек
- **Рекомендация**: 
  1. Выбрать одну таблицу для хранения (рекомендуется `UserPrivacySettings`)
  2. Мигрировать данные из второй таблицы
  3. Удалить дублирующееся поле из `UserProfileSettings`

### 5. Устаревшая таблица: **1**

#### UserSettings (user_settings)
- **Статус**: Частично используется (создается при регистрации, но не читается)
- **Проблема**: Заменена на специализированные таблицы:
  - `UserNotificationSettings` - настройки уведомлений
  - `UserPrivacySettings` - настройки приватности
  - `UserProfileSettings` - настройки профиля
- **Данные**: В таблице найдено 26 записей
- **Рекомендация**: 
  1. Мигрировать данные в специализированные таблицы (если нужно)
  2. Обновить код регистрации для создания записей в новых таблицах
  3. Удалить таблицу `UserSettings` после миграции

---

## 📋 ПЛАН ОПТИМИЗАЦИИ

### Приоритет 1: Критические дублирования

1. **Мигрировать User.skills → UserSkill**
   ```sql
   -- Скрипт миграции данных
   -- Выполнить перед удалением столбца
   ```

2. **Мигрировать Vacancy.skillsRequired → VacancySkill**
   ```sql
   -- Скрипт миграции данных
   -- Выполнить перед удалением столбца
   ```

### Приоритет 2: Оптимизация структуры

3. **Объединить profileVisibility**
   - Оставить только в `UserPrivacySettings`
   - Удалить из `UserProfileSettings`

4. **Удалить неиспользуемые столбцы**
   - `Vacancy.hoursPerWeek`
   - `Vacancy.contactEmail`
   - `Vacancy.telegramUsername`

### Приоритет 3: Очистка устаревших таблиц

5. **Удалить UserSettings**
   - После миграции данных в специализированные таблицы

---

## 🛠️ SQL МИГРАЦИИ

SQL миграции сохранены в файл: `database-optimization-migration.sql`

**⚠️ ВНИМАНИЕ**: Перед выполнением миграций:
1. Создайте резервную копию базы данных
2. Проверьте SQL запросы
3. Выполните миграцию данных перед удалением столбцов
4. Протестируйте приложение после изменений

---

## 📊 ИТОГО

- **Неиспользуемые таблицы**: 0
- **Неиспользуемые столбцы**: 3
- **Устаревшие столбцы**: 1
- **Дублирующиеся столбцы**: 2
- **Устаревшие таблицы**: 1

**Всего найдено проблем**: 7

---

## 💡 РЕКОМЕНДАЦИИ

1. **Немедленно**: Мигрировать данные из устаревших столбцов
2. **В ближайшее время**: Удалить дублирующиеся столбцы
3. **По мере необходимости**: Удалить неиспользуемые столбцы и таблицы

Скрипты проверки:
- `node check-database.js` - проверка базы данных
- `node optimize-database.js` - анализ и генерация миграций

